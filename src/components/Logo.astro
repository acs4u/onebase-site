---
// Wordmark placeholder in Poppins Medium. Replace with the official SVG wordmark from the brand guide
// (Canva "OneBase brand guide v.2" → Main logo). Never split "One" and "Base"; never shorten.
const { class: className = '' } = Astro.props;
---
<span class:list={['font-display font-medium tracking-tight text-[1.35rem] leading-none select-none', className]} aria-hidden="true">OneBase</span>
